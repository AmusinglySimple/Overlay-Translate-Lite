PySide6
Pillow
paddleocr
paddlepaddle>=2.6.0 # Or specific version if needed
langdetect # Note: langdetect-py is the package name, but import is langdetect
opencv-python
numpy
requests
keyring
sseclient-py # For AI streaming
psutil     # For resource monitoring
flask      # Assuming your app.py needs it
gunicorn   # Or whatever Flask server you use in app.py (optional if using Flask dev server)
argostranslate # Assuming Flask app uses this